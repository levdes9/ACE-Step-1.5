# Инструкция по запуску ACE-Step V1.5 на macOS (M2 Pro 16GB)

## 🚀 Быстрый запуск

```bash
cd "/Users/lev/Documents/AI Antigravity/models/music/ACE-Step/ACE-Step-V15"
export PATH="$HOME/.local/bin:$PATH"
uv run acestep --init_service true --offload_to_cpu true --device auto --language en
```

---

## ⚙️ Рекомендуемые настройки в интерфейсе

Для стабильной работы на Mac M2 Pro с 16GB:

| Параметр | Значение | Причина |
|----------|----------|---------|
| **Think** | ✅ Вкл | Улучшает качество |
| **ParallelThinking** | ❌ **Выкл** | Экономит память |
| **CaptionRewrite** | ✅ Вкл | Улучшает промпты |
| **AutoGen** | ❌ Выкл | Экономит память |
| **LM CFG Scale** | `1.0` | Стабильность |
| **Audio Duration** | до **60 сек** | 16GB лимит |
| **Batch Size** | `1` | Экономит память |
| **Inference Steps** | `8` | Стандарт turbo |

> ⚠️ **Важно**: При 16GB памяти LLM модель выгружается на CPU между запусками (параметр `--offload_to_cpu true`). Это немного замедляет работу, но предотвращает ошибки памяти.

---

## 📋 Первоначальная установка

### 1. Установите uv
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc && source ~/.zshrc
```

### 2. Синхронизируйте зависимости
```bash
cd "/Users/lev/Documents/AI Antigravity/models/music/ACE-Step/ACE-Step-V15"
uv sync
```

### 3. Запустите
```bash
uv run acestep --init_service true --offload_to_cpu true --device auto --language en
```

---

## 🔧 Решение проблем

**Ошибка "MPS out of memory":**
- Уменьшите **Audio Duration** до 30-60 секунд
- Убедитесь что `--offload_to_cpu true`
- Закройте другие приложения

**Медленная генерация:**
- Это нормально для CPU offload на 16GB
- Генерация 60 сек трека занимает ~3-5 минут

---

*Интерфейс доступен по адресу: http://127.0.0.1:7860*
